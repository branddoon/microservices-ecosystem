package com.app.products;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration tests for {@link SpringbootServicioProductosApplication}.
 *
 * <p>Verifies that the Spring application context loads correctly, including
 * all beans, configurations, and the H2 in-memory database setup.
 *
 * @author formacionbdi
 * @version 1.0.0
 */
@SpringBootTest
class SpringbootServicioProductosApplicationTests {

    /**
     * Verifies that the Spring application context starts without errors.
     */
    @Test
    void contextLoads() {
    }
}
